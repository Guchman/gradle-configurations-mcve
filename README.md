# gradle-configurations-mcve

To check invariants run:

```shell
./gradlew checkGwtSources
```

All gwt gradle plugins is under `buildSrc`
Check `CheckGwtSourcesTask.kt` for more details on invariants 